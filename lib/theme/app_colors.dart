import 'package:flutter/material.dart';

class AppColors {
  // Main colors
  static const Color primary = Color(0xFF121A21);
  static const Color secondary = Color(0xFF243647);

  // Background colors
  static const Color backgroundColor = Color(0xFF121A21);

  // Text colors
  static const Color primaryTextColor = Color(0xFFFFFFFF);
  static const Color secondaryTextColor = Color(0xFF94ADC7);

  // Button colors
  static const Color buttonColor = Color(0xFF338AE8);

  // Utility colors
  static const Color error = Color(0xFFD32F2F);
  static const Color success = Color(0xFF388E3C);
  static const Color warning = Color(0xFFF57C00);
  static const Color info = Color(0xFF1976D2);

  static const Color inactive = Color(0xFFBDBDBD);

  // Static colors
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF000000);
  static const Color shadow = Color(0xFF000000);
}
